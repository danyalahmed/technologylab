[controlplane]
%{ for ip in controlplane_ips ~}
${ip}
%{ endfor ~}

[workers]
%{ for ip in worker_ips ~}
${ip}
%{ endfor ~}

[k8s_nodes:children]
controlplane
workers

[all:vars]
ansible_user=danny
ansible_ssh_private_key_file=~/.ssh/id_rsa